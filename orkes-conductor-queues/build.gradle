dependencies {

    //implementation project(':orkes-conductor-redis-configuration')
    //implementation project(':orkes-conductor-oss-core')

    //Metrics
    //implementation "io.micrometer:micrometer-core:${versions.revMicrometerVersion}"

    implementation "redis.clients:jedis:${versions.revJedis}"
    //Guava
    implementation "com.google.guava:guava:${versions.revGuava}"

    //Caffeine
    implementation "com.github.ben-manes.caffeine:caffeine"

    compileOnly 'org.projectlombok:lombok:1.18.36'
    annotationProcessor 'org.projectlombok:lombok:1.18.36'
    testAnnotationProcessor 'org.projectlombok:lombok:1.18.36'

    testImplementation "org.testcontainers:testcontainers:1.17.2"
    testImplementation "org.junit.jupiter:junit-jupiter"
}

test {
    useJUnitPlatform()
}